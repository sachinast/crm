// User provisioning (no self-registration) + role assignment + IP whitelisting. Phase 1.
export default function AdminUsersPage() {
  return <p className="text-sm text-neutral-500">User provisioning — Phase 1.</p>;
}
