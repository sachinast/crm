// Lead list + filters (Date/Email/Mobile) — matches GET /leads in TECHNICAL_SPEC.md §5.
// Phase 2 in the delivery plan.
export default function LeadsPage() {
  return <p className="text-sm text-neutral-500">Lead list — Phase 2.</p>;
}
