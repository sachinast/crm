/**
 * Placeholder — replace by generating from the backend's OpenAPI schema once the API
 * surface is non-trivial (Phase 1+):
 *
 *   npx openapi-typescript http://localhost:8000/openapi.json -o types/api.d.ts
 *
 * See docs/TECHNICAL_SPEC.md §9.4.
 */
export {};
