fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 predates bcrypt 4.1's API changes (AttributeError on __about__,
# plus a wrap-bug self-test that mis-fires) — pin bcrypt back until passlib ships a fix.
bcrypt==4.0.1
python-multipart==0.0.20
celery==5.4.0
redis==5.2.1
httpx==0.28.1
greenlet==3.1.1
pytest==8.4.2
# pytest-asyncio 1.4.0 requires pytest>=8.4,<10 — bumped together from 8.3.4 to
# keep these two resolvable (was passing locally only because this dev venv had
# already drifted above the old pin; a fresh `pip install -r requirements.txt`,
# same as CI does, failed to resolve until this bump).
pytest-asyncio==1.4.0
email-validator==2.2.0
